import { amo } from './amo';
import { crx } from './crx';
import { cws } from './cws';
import { edge } from './edge';
import { xpi } from './xpi';

export { amo, crx, cws, edge, xpi };

export const platforms = {
  amo,
  cws,
  xpi,
  edge,
  crx,
};
